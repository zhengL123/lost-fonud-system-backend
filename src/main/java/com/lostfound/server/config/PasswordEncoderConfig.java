package com.lostfound.server.config;

import org.springframework.context.annotation.Configuration;

/**
 * 密码编码器配置类
 */
@Configuration
public class PasswordEncoderConfig {
    // passwordEncoder bean 已经在 SecurityConfig 中定义，这里不再重复定义
}